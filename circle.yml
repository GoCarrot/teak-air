machine:
  xcode:
    version: 7.3
test:
  override:
  - ./compile
dependencies:
  # override:
  pre:
  - ./setup
  # bundler:
  #   without: [production, osx]
  cache_directories:
    - "air-sdk"
    - "tmp"
    - "../teak-ios"
    - "../teak-android"
    - "/usr/local/Cellar/android-sdk"
