machine:
  xcode:
    version: 8.2.1
test:
  override:
  - ./compile
dependencies:
  # override:
  pre:
  - ./setup
  # bundler:
  #   without: [production, osx]
  cache_directories:
    - "air-sdk"
    - "../teak-ios"
    - "../teak-android"
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync ./bin/io.teak.sdk.Teak.ane s3://air/io.teak.sdk.Teak.ane --delete
